export default {
  setPreset(state, preset) {
    state.preset = preset;
  },

  setMeme(state, meme) {
    state.meme = meme;
  },

  setFontFamilies(state, fontFamilies) {
    state.fontFamilies = fontFamilies;
  }
};
